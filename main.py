# import asyncio
# from telegram import Bot
# import os
# from dotenv import load_dotenv

# load_dotenv()

# TOKEN = os.getenv("TELEGRAM_KEY")

# async def send():
#     bot = Bot(TOKEN)
#     await bot.sendMessage(chat_id=5761507238, text=f"wants to user the Bot")

# asyncio.run(send())